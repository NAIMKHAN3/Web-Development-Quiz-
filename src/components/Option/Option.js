import React from 'react';

const Option = ({ option }) => {
    return (
        <div>
            {/* <input type="radio"> {option}</input> */}
            <p>{option}</p>
        </div>
    );
};

export default Option;